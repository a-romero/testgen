fastapi>=0.110,<1.0
uvicorn[standard]>=0.27
pydantic>=2.5,<3.0
python-multipart>=0.0.9
# Optional LLM providers — generation falls back to a deterministic engine when
# these are absent or no API key is configured.
openai>=1.0
anthropic>=0.25
